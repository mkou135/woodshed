/**
 * Derived degree-pattern frequencies for common-language identification.
 * Regenerate with `npm run corpus:licks` (reads the Weimar Jazz Database and
 * the Bopland licks from ~/dev/woodshed-data — aggregate statistics only,
 * DECISIONS 2026-08-27 "Corpus-derived lick table"). This stub is empty until
 * the first generation.
 */
export const LICK_WJD_SOLOS = 0
export const LICK_BOP_LICKS = 0

export const LICK_PATTERNS: Record<string, { wjd: number; bop: number }> = {}
